{
  "version": "1.0.0",
  "canvas": ".principal-views/workflow-scenarios-panel/workflow-scenarios-panel.otel.canvas",
  "name": "Panel Usage",
  "description": "User interactions with the Workflow Scenarios Panel for viewing and analyzing traces",
  "spanPattern": "workflow-scenarios-panel.session",
  "scenarioSelection": "first-match",
  "status": "draft",
  "scenarios": [
    {
      "id": "scenario-hover-preview",
      "priority": 1,
      "description": "User hovers a scenario to preview its nodes with viewport fit",
      "template": {
        "events": {
          "scenario.hovered": "Hovering scenario: {{scenario.name}} ({{scenario.id}}, state={{hover.state}})",
          "canvas.focus.changed": "Preview focus on {{focus.node.count}} nodes (source={{focus.source}}, events={{focus.event.names}})",
          "viewport.fitted": "Viewport fitted to hovered scenario nodes ({{fit.node.count}} nodes, padding={{fit.padding}}, trigger={{fit.trigger}})"
        },
        "summary": "Previewed {{scenario.name}} scenario"
      }
    },
    {
      "id": "scenario-click-focus",
      "priority": 2,
      "description": "User clicks a scenario to focus canvas on its nodes",
      "template": {
        "events": {
          "scenario.selected": "Selected scenario: {{scenario.name}} ({{scenario.id}}) from workflow {{workflow.id}}",
          "canvas.focus.changed": "Focused on {{focus.node.count}} scenario nodes (source={{focus.source}}, events={{focus.event.names}})",
          "viewport.fitted": "Viewport fitted to focused nodes ({{fit.node.count}} nodes, padding={{fit.padding}}, trigger={{fit.trigger}})",
          "node.highlighted": "Highlighted node {{node.id}} for {{node.event}} (source={{highlight.source}})"
        },
        "summary": "Focused canvas on {{scenario.name}} scenario"
      }
    },
    {
      "id": "trace-loaded-focus",
      "priority": 3,
      "description": "Trace loads and auto-focuses to matched scenario nodes",
      "template": {
        "events": {
          "trace.selected": "Selected trace {{trace.id}} from {{trace.source}} ({{span.count}} spans, scenario={{scenario.id}})",
          "trace.loaded": "Loaded trace {{trace.id}}: {{span.count}} spans with {{event.count}} events (match={{match.type}}, coverage={{coverage.percent}}%)",
          "canvas.focus.changed": "Focused on {{focus.node.count}} matched nodes (source={{focus.source}}, events={{focus.event.names}})",
          "viewport.fitted": "Viewport fitted to matched scenario ({{fit.node.count}} nodes, padding={{fit.padding}}, trigger={{fit.trigger}})"
        },
        "summary": "Loaded trace and focused on matched scenario"
      }
    },
    {
      "id": "node-inspection",
      "priority": 4,
      "description": "User clicks a node to highlight it and focus viewport on it",
      "template": {
        "events": {
          "node.clicked": "Clicked node {{node.id}} (event={{node.event}}, shift={{modifier.shift}})",
          "node.highlighted": "Highlighted node {{node.id}} for {{node.event}} (source={{highlight.source}})",
          "viewport.fitted": "Viewport fitted to clicked node ({{fit.node.count}} nodes, padding={{fit.padding}}, trigger={{fit.trigger}})"
        },
        "summary": "Inspected node {{node.id}}"
      }
    },
    {
      "id": "event-navigation",
      "priority": 5,
      "description": "User clicks an event in the timeline to highlight its node",
      "template": {
        "events": {
          "event.clicked": "Clicked event {{event.name}} at index {{event.index}} (span={{span.index}})",
          "node.highlighted": "Highlighted node for {{node.event}} (source={{highlight.source}})"
        },
        "summary": "Navigated to event {{event.name}}"
      }
    },
    {
      "id": "trace-playback",
      "priority": 6,
      "description": "User plays back a trace execution",
      "template": {
        "events": {
          "trace.loaded": "Loaded trace {{trace.id}}: {{span.count}} spans with {{event.count}} events (match={{match.type}}, coverage={{coverage.percent}}%)",
          "playback.started": "Started playback at span {{span.index}} (trace={{trace.id}}, event={{event.index}})",
          "node.highlighted": "Highlighting node for current event (source={{highlight.source}})",
          "playback.stopped": "Playback {{reason}} at event {{event.index}} (trace={{trace.id}}, span={{span.index}})"
        },
        "summary": "Played back trace with {{span.count}} spans"
      }
    },
    {
      "id": "source-navigation",
      "priority": 7,
      "description": "User navigates to source code from the panel",
      "template": {
        "events": {
          "node.clicked": "Clicked node {{node.id}} (event={{node.event}}, shift={{modifier.shift}})",
          "node.highlighted": "Highlighted node {{node.id}} (source={{highlight.source}})",
          "source.opened": "Opened source: {{source.path}} (trigger={{trigger}})"
        },
        "summary": "Navigated to source {{source.path}}"
      }
    },
    {
      "id": "explore-scenario",
      "priority": 8,
      "description": "User loads panel and explores a scenario with its traces",
      "template": {
        "events": {
          "panel.loading": "Loading panel {{panel.id}} for canvas {{canvas.id}} with workflow {{workflow.id}}",
          "panel.loaded": "Panel {{panel.id}} ready: {{canvas.name}} / {{workflow.name}} with {{scenario.count}} scenarios and {{trace.count}} traces",
          "scenario.selected": "Selected scenario: {{scenario.name}} ({{scenario.id}}) from workflow {{workflow.id}}",
          "canvas.focus.changed": "Focused on scenario nodes ({{focus.node.count}} nodes, source={{focus.source}}, events={{focus.event.names}})",
          "viewport.fitted": "Viewport fitted to scenario ({{fit.node.count}} nodes, padding={{fit.padding}}, trigger={{fit.trigger}})",
          "trace.selected": "Viewing trace {{trace.id}} from {{trace.source}} ({{span.count}} spans, scenario={{scenario.id}})"
        },
        "summary": "Explored scenario {{scenario.name}} with trace analysis"
      }
    },
    {
      "id": "load-error",
      "priority": 10,
      "description": "Panel failed to load canvas or workflow",
      "template": {
        "events": {
          "panel.loading": "Loading panel {{panel.id}} for canvas {{canvas.id}} with workflow {{workflow.id}}",
          "panel.error": "Error loading panel {{panel.id}}: {{error.message}} ({{error.type}})"
        },
        "summary": "Failed to load: {{error.message}}"
      }
    }
  ]
}
